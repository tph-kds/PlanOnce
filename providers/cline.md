# Cline adapter

- **Ask human:** use Cline's interactive approval/chat surface; fallback to a normal user turn.
- **Read/write:** use repository/file editing capabilities.
- **Run command:** use the terminal/shell tool exposed by Cline.
- Fresh context: Plan/Act-style modes may help separate thinking and execution, but PlanOnce remains the planning authority; use sequential compact handoffs if no isolated workers exist.
- `npx skills` target ID: `cline`; current Skills CLI uses portable `.agents/skills/` for both project compatibility and the generic global Agent Skills location.
- **Fallback:** generic capability contract.
