# Cursor adapter

- **Ask human:** use the interactive conversation/approval surface.
- **Read/write:** use Cursor's repository editing capabilities.
- **Run command:** use terminal execution when available.
- Fresh context: delegate only if the active mode supports safe independent workers; otherwise use sequential waves.
- `npx skills` target ID: `cursor`; current Skills CLI uses portable `.agents/skills/` for project scope and `~/.cursor/skills/` globally.
- Keep project rules/AGENTS instructions above PlanOnce planning artifacts in authority.
- **Fallback:** generic capability contract.
