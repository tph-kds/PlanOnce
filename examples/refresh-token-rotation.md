# Example

For an established service:

```text
Use planonce-brown-normal to implement refresh-token rotation.
```

Expected behavior: inspect current auth first → create one plan → human approval → bounded implementation waves → regression/integration verification → human ship gate.
