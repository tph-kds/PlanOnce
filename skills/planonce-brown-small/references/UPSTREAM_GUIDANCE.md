# Pinned methodology guidance

**Agent OS:** `buildermethods/agent-os` **v3.0.0** (`809fb4e`)  
**GSD Core:** `open-gsd/gsd-core` **v1.12.0** (`ceed559`)

These are vendored methodology/runtime sources, not separate end-user prerequisites. PlanOnce compiles their useful behavior into provider-neutral capabilities and keeps raw upstream surfaces subordinate to PlanOnce.

Use Agent OS-style standards discovery/injection on representative existing code. Borrow GSD interactive execution for a small bug/fix path. Existing behavior and analogous code are evidence; do not re-plan or modernize unrelated code.
