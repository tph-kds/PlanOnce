# Compiled Upstream Runtime Contract

PlanOnce is the orchestration authority. The router may inspect repository/project context using Agent OS-style standards awareness and GSD Core-style brownfield/onboarding discipline, but it must not invoke raw Agent OS or GSD commands. Classification ends once a canonical PlanOnce implementation skill is selected.
