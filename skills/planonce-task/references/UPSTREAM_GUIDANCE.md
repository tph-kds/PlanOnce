# Upstream Guidance

PlanOnce pins Agent OS `v3.0.0` and GSD Core `v1.12.0` as engineering-methodology inputs. `planonce-task` uses only their high-level project/context and brownfield-risk ideas to classify work. It does not expose raw upstream planning commands and does not require either upstream to be installed separately.
